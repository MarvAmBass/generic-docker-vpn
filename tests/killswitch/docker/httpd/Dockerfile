# Minimal busybox httpd image, used both for the fake-internet target and as
# the jailed stand-in service (any real container works the same way).
FROM alpine:latest

RUN apk add --no-cache busybox-extras
